window.onload = function(){
    setDefaultDates();
};

// set maximum wide default date range values on inputs
// this will make sure no filtering when page loads and all table rows appear
function setDefaultDates(){
    // document.getElementById('dateFromInput').valueAsDate = new Date(2000, 1, 1, 0, 0, 0);
    // document.getElementById('dateToInput').valueAsDate = new Date();
    //
    // var dateToValue = document.getElementById('dateToInput').value;
    // var dateFromValue = document.getElementById('dateFromInput').value;
    //
    // console.log(dateToValue);
}
